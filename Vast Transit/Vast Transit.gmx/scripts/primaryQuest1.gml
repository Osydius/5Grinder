var currentStage = argument[0];
var currentStageComplete = argument[1];

switch (currentStage) {
    case 1: {
        /*draw_text(view_xview[0]+100,view_yview[0]+100,"Go north to the planet hub.");
        if room == findRoomByString("planet1_hub") {
            updatePrimaryQuest("primaryQuest1",2,0);
        }*/
        break;
    }
    case 2: {
            draw_text(view_xview[0]+100,view_yview[0]+100,"COMPLETED QUEST 1");
        break;
    }
}
