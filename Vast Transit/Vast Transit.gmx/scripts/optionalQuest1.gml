var questStage = argument[0];
var questStageComplete = argument[1];

var questName = "optionalQuest1";

switch (questStage) {
    case 0: {
        // make quest active
        break;
    }
    case 1: {
        // stage one of quest
        break;
    }
    case 2: {
        // stage two of quest
        break;
    }
}

